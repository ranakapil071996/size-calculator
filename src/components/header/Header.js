import React from 'react';
import { AppBar } from '@material-ui/core';

export default function Header() {
  return (
    <AppBar style={{ padding: 20 }} color='primary'>
      Size calculator
    </AppBar>
  );
}
